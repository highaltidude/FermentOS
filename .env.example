DATABASE_URL=postgresql://brewmaster:your_password_here@localhost:5432/brewmaster
SESSION_SECRET=change_this_to_a_long_random_string
NODE_ENV=production
PORT=8080

# Docker only — generated automatically by docker-install.sh
# Set these before running docker compose if not using the install script
HOST_PORT=3000
DB_PASSWORD=change_this_password
SESSION_SECRET=change_this_to_a_long_random_string
